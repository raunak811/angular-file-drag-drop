import { NgFileDragDropDirective } from './ng-file-drag-drop.directive';

describe('NgFileDragDropDirective', () => {
  it('should create an instance', () => {
    const directive = new NgFileDragDropDirective();
    expect(directive).toBeTruthy();
  });
});
